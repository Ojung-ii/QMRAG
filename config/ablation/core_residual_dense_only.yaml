extends: ../default.yaml

run:
  ablation_variant: core_residual_dense_only
  enable_timing: true

generation:
  prompt_profile: common_qa
  rendering_profile: structured_chain

retrieval:
  retrieval_variant: full_hetero
  seed_selection_variant: global_seed_search
  residual_selection: residual_dense_only
  candidate_cap:
    enabled: false
  bridge:
    enabled: true
    selection: residual_dense_only
    ordering: anchor_chain_aware
    multi_anchor_bundle: true

