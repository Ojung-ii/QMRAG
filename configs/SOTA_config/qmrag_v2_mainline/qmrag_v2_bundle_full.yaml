extends: qmrag_v2_common_full.yaml

generation:
  prompt_profile: qmrag_bundle_qa
  rendering_profile: structured_chain
  token_counting:
    enabled: true
    tokenizer_model: auto
    fallback_to_estimate: true

sota_config:
  role: prompt_ablation_bundle_full
  prompt_experiment_type: ablation
  context: full
  top_bundles: null
  context_token_budget: null
  notes:
    - Same retrieval, ordering, rendering, and context as mainline.
    - QMRAG-bundle prompt is an ablation/upper-bound for structured evidence utilization.
