extends: ace_rag_common_full.yaml

generation:
  prompt_profile: ace_rag_bundle_qa
  rendering_profile: structured_chain
  token_counting:
    enabled: true
    tokenizer_model: auto
    fallback_to_estimate: true

sota_config:
  role: prompt_ablation_bundle_full
  prompt_experiment_type: ablation
  context: full
  top_bundles: null
  context_token_budget: null
  notes:
    - Same retrieval, ordering, rendering, and context as mainline.
    - ACE-RAG-bundle prompt is an ablation/upper-bound for structured evidence utilization.
